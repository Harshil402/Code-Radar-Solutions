#include <stdio.h>

int main() {
    float num1, num2;

    // Taking two space-separated floating-point numbers as input
    scanf("%f %f", &num1, &num2);

    // Printing the product of the two numbers
    printf("Product: %.2f\n", num1 * num2);

    return 0;
}

}