#include <stdio.h>

int main() {
    float radius, area;
    
    // Taking the radius of the circle as input
    scanf("%f", &radius);
    
    // Calculating the area of the circle with π as 3.14
    area = 3.14 * radius * radius;
    
    // Printing the area of the circle
