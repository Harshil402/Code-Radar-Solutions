#include <stdio.h>

int main() {
    int num;

    // Taking an integer as input
    scanf("%d", &num);

    // Printing the hexadecimal representation
    printf("Hexadecimal: %X\n", num);

    // Printing the octal representation
    printf("Octal: %o\n", num);

    return 0;
}
